name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest  

    steps:
      - name: Setup MSYS2
      - uses: msys2/setup-msys2@v2
      - uses: actions/checkout@v2
      - run: mingw32-make
